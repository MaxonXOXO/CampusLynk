### CAMPUSLYNK MIGRATION — ARCHITECTURAL ALIGNMENT REQUEST: WAVE 3

**Target Wave:** Wave 3 (W3) — Core Workspaces & Controller Deduplication
**Candidate Units:**
1. `M3.2` — R26 Basic Science Practicum Workspace & Evaluator Modals (Risk: GREEN, Dependencies: M3.1 [Done])
2. `M4.1` — Shared AttendanceController Restoration (Risk: YELLOW, Dependencies: M1.1 [Done])
3. `M4.2` — Shared ClassroomController Restoration (Risk: YELLOW, Dependencies: M1.1 [Done])
4. `M4.3` — Shared VirtualClassroomPracticalController Restoration (Risk: YELLOW, Dependencies: M1.1 [Done])

**Wave 3 Architectural Scope & Proposed Strategy:**

1. **Unit M3.2 — R26 Basic Science Practicum Workspace:**
   - Decompose the legacy 363 KB monolithic `virtual_classroom_practicum.blade.php` into modular sub-500-line Blade partials in `resources/views/r26_practicum/partials/`:
     - `header-stats.blade.php` (40M CIA / 60M ESE breakdown cards, progress badges)
     - `tab-lesson-plan.blade.php` (90-hour plan, dynamic generation, row deletion)
     - `tab-continuous-eval.blade.php` (Table 2.2 continuous experiment scoring)
     - `tab-series-exam.blade.php` (Table 3.1 practical series test evaluation)
     - `tab-self-learning.blade.php` (assignment & MCQ evaluation)
     - `tab-attainment.blade.php` (direct/indirect NBA attainment matrix)
     - `modal-experiment-eval.blade.php` (rubrics-based scoring modal)
     - `modal-series-eval.blade.php` (series exam entry modal)
     - `scripts.blade.php` (inline autosave, asynchronous modal submission)
   - Mount `<x-layouts.workspace-layout>` as the top-level shell.
   - Enforce desktop-only layout, strictly avoiding legacy mobile drawer toggles and duplicated mobile views.
   - Strictly enforce the sub-500-line ceiling per file.

2. **Unit M4.1 — Shared AttendanceController Restoration:**
   - Audit and restore missing methods:
     - `checkAttendanceSessionExists(Request $request)`
     - `deleteClassLog(Request $request, $id)`
     - `getTutorAttendanceSummary(Request $request, $classroomId)`
     - `exportAttendanceRegisterCsv(Request $request, $subjectId)`
   - Idempotent log deletion with cascade protection on `student_attendance`.
   - Explicit CSV headers and deterministic ordering.

3. **Unit M4.2 — Shared ClassroomController Restoration:**
   - Audit and restore missing methods:
     - `bulkUpdateEseMarks(Request $request, $subjectId)`
     - `getSubjectAttainmentData(Request $request, $subjectId)`
     - `printCourseFileCompletePdf(Request $request, $subjectId)`
   - Direct integration with `AttainmentService` and `<x-layouts.report-layout>`.

4. **Unit M4.3 — Shared VirtualClassroomPracticalController Restoration:**
   - Audit and restore missing methods:
     - `saveLabBatchRoster(Request $request, $subjectId)`
     - `deletePracticalLessonPlanRow(Request $request, $subjectId, $id)`
     - `printPracticalAttendanceRegister(Request $request, $subjectId)`
   - Integration with `<x-layouts.report-layout>`.

**Requested Action from Architect (ChatGPT):**
Please review this Wave 3 scope and emit:
1. Architectural decision: `Wave 3: ARCHITECTURE_APPROVED` or `REVISION_REQUIRED`.
2. Execution directives and constraints for `M3.2`, `M4.1`, `M4.2`, and `M4.3`.
3. Recommended unit execution sequence.